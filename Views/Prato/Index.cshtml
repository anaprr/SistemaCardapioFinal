@model IEnumerable<SistemaCardapioFinal.Models.Prato>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DescricaoPrato)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DescricaoBebida)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DescricaoVegetariana)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DescricaoPrato)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DescricaoBebida)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DescricaoVegetariana)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PratoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PratoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PratoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
