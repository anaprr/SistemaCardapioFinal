@model SistemaCardapioFinal.Models.Cardapio

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Cardapio</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="CardapioId" />
            <div class="form-group">
                <label asp-for="CardapioNome" class="control-label"></label>
                <input asp-for="CardapioNome" class="form-control" />
                <span asp-validation-for="CardapioNome" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="PratoId" class="control-label"></label>
                <select asp-for="PratoId" class="form-control" asp-items="ViewBag.PratoId"></select>
                <span asp-validation-for="PratoId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
